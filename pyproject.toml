[project]
name="lana_lyric_analysis"
version="1.0.0"
requires-python=">=3.11.0"
dependencies =[
    "jupyter",
    "matplotlib",
    "nltk",
    "numpy",
    "pandas",
    "plotly",
    "pyarrow",
    "seaborn",
    "scikit-learn",
    "streamlit",
    "textblob",
    "textstat",
    "wordcloud",
    "vadersentiment"
]

[pyproject.optional-dependencies]
dev = [
    "pytest"
]

[build-system]
requires = [
    "setuptools>=68",
    "wheel"
]

build-backend="setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]